# from pydantic import BaseModel
#
# from web.models.events import MatchFinished
#
#
# class ImageManager:
#
#     @staticmethod
#     async def process_image(stats: BaseModel):
#         if isinstance(stats, MatchFinished):
#             MatchFinishedImageManager.process_image(stats)
#         elif isinstance(stats, )
